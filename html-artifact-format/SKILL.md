---
name: html-artifact-format
description: >
  AI向け成果物をMarkdownのまま残すか、人間向けレビュー・共有成果物として単一HTMLへ変換するかを判断し、内容規模に応じて single-screen-brief / scroll-report / tabbed-workspace のHTML構成を選んで生成する時に使う。HTML化する場合は、SVG図・色分けテーブル・カード・Before/After比較などの視覚化要素を素材に合わせて組み込み、文字だけの逐語変換にはしない。プラン、Issue、レビューまとめ、レポート、ポストモーテム、教育資料、軽量プロトタイプを人間が読みやすいHTMLにしたい時に使う。
---

# 概要

AIが読むものはMarkdown、人間がレビュー・比較・共有するものはHTML、という基準で成果物の形式を決める。HTMLにする場合は、内容規模に応じて構成テンプレートを選び、SVG図・色分けテーブル・カードなどの**視覚化要素**で「読まなくても構造が分かる」状態に仕上げる。外部依存のない単一HTMLファイルにする。

**markdownを `<h2>` `<p>` `<ul>` に置き換えただけのHTMLは禁止。** 文字情報を減らし、図・表・カード・色分けで構造を伝える。

# 使用タイミング

- ユーザーがプラン、Issue、レビュー結果、調査メモ、議事録、ポストモーテム、教育資料を人に共有しやすい形へ整えたい時
- ユーザーが「HTML化」「人間向け資料」「共有用ページ」「レビュー用ページ」「見やすく整理」と依頼した時
- 他スキルがMarkdownの設計プランやIssue本文を作成した後、人間向けHTMLも作るか確認して承諾された時
- 成果物をMarkdownのままにするべきか、HTMLにするべきか判断が必要な時

# Agentが行うこと

1. 成果物の主な読者がAIか人間かを判定する。
2. Markdownのまま残すべき場合は、HTML化しない理由を短く伝える。
3. 人間向けHTMLが有効な場合は、`references/template-selection.md` を読み、HTMLテンプレートを1つ選ぶ。
4. 入力素材を人間が読む順序へ再構成する。**入力素材に書かれていない事実情報（担当者名、Slack/チケットID、重大度ラベル、コード断片、URL、数値、状態など）を視覚化のために創作してはいけない。**
5. `references/visual-components.md` を読み、テンプレートと成果物種別に合う視覚化コンポーネントを選ぶ。素材にない情報が必要なコンポーネントは、無理に埋めず省略するか `不明 (要追記)` と明示する。
6. `references/implementation-quality.md` を読み、HTML作成ルールと品質チェックを満たす。
7. `mkdir -p ./output` で保存先を用意し、`./output/{slug}.html` に単一HTMLファイルを作成する。
8. 生成後、保存先・選んだテンプレート・使用した視覚化コンポーネントを報告する。素材にない情報を `不明 (要追記)` で表示した場合は、その箇所をユーザーへの報告で1行明示する。

# 入力 / 出力

## 入力

- Markdown原稿、設計プラン、GitHub Issue本文、PR/レビューまとめ、議事録、調査メモ
- 共有対象、想定読者、用途、必要なら作成済みIssueやPRのURL
- 他スキルから連携される場合は、確定済み `<proposed_plan>` と作成済みIssue本文

## 出力

- Markdown継続を推奨する場合: 理由と次アクション
- HTMLを生成する場合: `./output/{slug}.html`
- 完了報告: 選択テンプレート、保存先、使用した視覚化コンポーネント、主な構成

## 入力素材が不足している場合

- HTML生成を明示されていても、本文・原稿・Issue本文などHTMLに入れる実素材がない場合は、ファイルを作らず「生成方針サマリ」を返す。
- 生成方針サマリには、HTML化する/しないの判断、選択テンプレート、想定する視覚化コンポーネント、想定保存先、主な構成、実素材が必要である旨を含める。
- 実素材不足を理由に生成方針サマリで止める判断は、例外処理として明示されているため、不明瞭点ではなく実行結果として扱う。
- 実素材がある場合は、方針サマリだけで止めずに `./output/{slug}.html` を作成する。

# 参照資料

- 形式判断とテンプレート選択: `references/template-selection.md`
- 視覚化コンポーネント、CSS/SVGスターター、成果物種別ごとの推奨: `references/visual-components.md`
- HTML作成ルール、GitHub Issue連携、NGパターン、品質チェック: `references/implementation-quality.md`
