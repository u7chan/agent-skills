name: Validate Skills

on:
  pull_request:
  push:

jobs:
  validate-skills:
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v4
      - name: Validate skills
        run: bash .scripts/validate-skills.sh
