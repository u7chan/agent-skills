interface:
  display_name: "Cagent Agent Command Resolve"
  short_description: "cagentの実効値を起動コマンドと委譲メタ情報へ固定"
  default_prompt: "Use $cagent-agent-command-resolve to freeze cagent Agent, Model, and Effort into a launch command and optional delegation metadata without starting an agent."
