interface:
  display_name: "GitHub PR Review"
  short_description: "指定されたGitHub PRをレビューしてPR上にコメントを投稿する"
  default_prompt: "Use $github-pr-review to review a GitHub PR and include AI identity only when the current Herdr delegation metadata is present."
