interface:
  display_name: "Herdr GitHub PR Orchestrate"
  short_description: "Herdr Agentへ実装・レビューを委譲し共通SkillでPRを作成"
  default_prompt: "Use $herdr-github-pr-orchestrate to delegate implementation and review through Herdr while using shared skills for scoped commits and PR creation."
