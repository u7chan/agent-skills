# 回答

日本語で簡潔かつ丁寧に回答してください。

## GitHub PRレビューコメント

GitHub PRへレビューコメントまたは返信を投稿するとき、AI識別メタ情報には現在のCodexの実行モデルを明記します。投稿直前に、現在Agent自身について次の優先順位でモデルを解決してください。

1. 現在のAgentセッションが提供する明示的な実行モデル
2. 現在Agentを起動したHerdr/cagentが明示またはdoctor / dry-runで解決した実行モデル
3. 1と2を取得できないCodexに限り、`/home/u7dev/.codex/config.toml` の `model`
4. すべて取得不能なら不明とし、モデル名を推測しない

Herdr/cagentの実行モデルとCodex Configが異なる場合は、実行モデルを使ってください。会話開始時や過去に取得した値は再利用しません。

- 通常コメント: `AIレビュー補助（Codex / <model>）によるレビューです 🤖`
- 再チェックコメント: `AIレビュー補助（Codex / <model>）による再チェックコメントです 🤖`
- モデルが不明な場合: `AIレビュー補助（Codex）によるレビューです 🤖`
