interface:
  display_name: "AI Identity Resolve"
  short_description: "実行モデル優先でレビュー用AI識別メタデータを解決する"
  default_prompt: "Use $ai-identity-resolve to resolve current AI identity metadata with runtime model priority."
